import * as dotenv from 'dotenv';
import { createClient } from 'contentful-management';

dotenv.config();

const createContentModel = async () => {
  // Your content model creation logic
};

createContentModel().catch(console.error); 